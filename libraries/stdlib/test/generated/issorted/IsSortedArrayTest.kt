/*
 * Copyright 2010-2026 JetBrains s.r.o. and Kotlin Programming Language contributors.
 * Use of this source code is governed by the Apache 2.0 license that can be found in the license/LICENSE.txt file.
 */


package test.generated.issorted

//
// NOTE: THIS FILE IS AUTO-GENERATED by the IsSortedTestGenerator.kt
// See: https://github.com/JetBrains/kotlin/tree/master/libraries/stdlib
//

import kotlin.test.*

class IsSortedArrayTest {

    @Test
    fun isSorted() {
        assertTrue(arrayOf<String>().isSorted())
        assertTrue(arrayOf("a").isSorted())
        assertTrue(arrayOf("a", "b", "c").isSorted())
        assertFalse(arrayOf("c", "b", "a").isSorted())
        assertFalse(arrayOf("b", "a", "c").isSorted())
        assertTrue(arrayOf("b", "b", "b").isSorted())
    }

    @Test
    fun isSortedDescending() {
        assertTrue(arrayOf<String>().isSortedDescending())
        assertTrue(arrayOf("a").isSortedDescending())
        assertTrue(arrayOf("c", "b", "a").isSortedDescending())
        assertFalse(arrayOf("a", "b", "c").isSortedDescending())
        assertFalse(arrayOf("b", "a", "c").isSortedDescending())
        assertTrue(arrayOf("b", "b", "b").isSortedDescending())
    }

    @Test
    fun isSortedWith() {
        assertTrue(arrayOf<String>().isSortedWith(naturalOrder()))
        assertTrue(arrayOf("a", "b", "c").isSortedWith(naturalOrder()))
        assertTrue(arrayOf("c", "b", "a").isSortedWith(reverseOrder()))
        assertFalse(arrayOf("b", "a", "c").isSortedWith(naturalOrder()))
        assertTrue(arrayOf("Apple", "banana", "Cherry").isSortedWith(String.CASE_INSENSITIVE_ORDER))
        assertFalse(arrayOf("banana", "Apple", "Cherry").isSortedWith(String.CASE_INSENSITIVE_ORDER))
    }

    @Test
    fun isSortedBy() {
        assertTrue(arrayOf<String>().isSortedBy { it })
        assertTrue(arrayOf("a", "bb", "ccc").isSortedBy { it.length })
        assertFalse(arrayOf("ccc", "bb", "a").isSortedBy { it.length })
    }

    @Test
    fun isSortedByDescending() {
        assertTrue(arrayOf<String>().isSortedByDescending { it })
        assertTrue(arrayOf("ccc", "bb", "a").isSortedByDescending { it.length })
        assertFalse(arrayOf("a", "bb", "ccc").isSortedByDescending { it.length })
    }

    @Test
    fun isSortedNaNDouble() {
        assertTrue(arrayOf(1.0, 2.0, Double.NaN).isSorted())
        assertFalse(arrayOf(Double.NaN, 1.0, 2.0).isSorted())
        assertFalse(arrayOf(1.0, Double.NaN, 2.0).isSorted())
        assertTrue(arrayOf(Double.NaN, Double.NaN).isSorted())
        assertTrue(arrayOf(Double.NaN, 2.0, 1.0).isSortedDescending())
        assertFalse(arrayOf(2.0, 1.0, Double.NaN).isSortedDescending())
    }

    @Test
    fun isSortedNegativeZeroDouble() {
        assertTrue(arrayOf(-0.0, 0.0).isSorted())
        assertFalse(arrayOf(0.0, -0.0).isSorted())
        assertTrue(arrayOf(0.0, -0.0).isSortedDescending())
        assertFalse(arrayOf(-0.0, 0.0).isSortedDescending())
    }

    @Test
    fun isSortedNaNFloat() {
        assertTrue(arrayOf(1.0f, 2.0f, Float.NaN).isSorted())
        assertFalse(arrayOf(Float.NaN, 1.0f, 2.0f).isSorted())
        assertFalse(arrayOf(1.0f, Float.NaN, 2.0f).isSorted())
        assertTrue(arrayOf(Float.NaN, Float.NaN).isSorted())
        assertTrue(arrayOf(Float.NaN, 2.0f, 1.0f).isSortedDescending())
        assertFalse(arrayOf(2.0f, 1.0f, Float.NaN).isSortedDescending())
    }

    @Test
    fun isSortedNegativeZeroFloat() {
        assertTrue(arrayOf(-0.0f, 0.0f).isSorted())
        assertFalse(arrayOf(0.0f, -0.0f).isSorted())
        assertTrue(arrayOf(0.0f, -0.0f).isSortedDescending())
        assertFalse(arrayOf(-0.0f, 0.0f).isSortedDescending())
    }
}
